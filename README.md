Тестовое задание для стажера по направлению Разработчик С++

Часть 1 - pt1
файлы:
.
├── CMakeLists.txt - симейк лист для динамической и статической сборки бибилиотеки без второй части и юнит-тестов
├── journal.cpp - файл с реализацией методов journal.hpp
├── journal.hpp - файл с телом journal и декларацией методов
└── main.cpp - файл с проверкой работы инициализации журнала и сохранения сообщения

запуск:
```
создаём папку сборки
mkdir build
переходим в неё
cd build
создание файлов для сборки
cmake ..
сборка библиотеки и проекта
make
запуск
./main
```

Часть 2 - pt2:
файлы:
.
└── main.cpp - консольное приложение для многопоточной записи сообщений в файл через журнал

запуск:

запуск:
```
выходим из папки pt2
cd ~/Test_C-_Infotecs/
создаём папку сборки
mkdir build
переходим в неё
cd build
создание файлов для сборки
cmake ..
сборка библиотеки и проекта
make
запуск
./main параметр1 параметр2
```

пояснение: параметр1 - название журнала и файла, в который будут записываться сообщения, параметр2 - минимальный уровень важности сообщений

Юнит-тесты - tests
.
└── test-journal.cpp - файл с юнит-тестами для журнала

запуск:
```
выходим из папки test
cd ~/Test_C-_Infotecs/
создаём папку сборки
mkdir build
переходим в неё
cd build
создание файлов для сборки
cmake ..
сборка библиотеки и проекта
make
запуск
./jour_test
```
